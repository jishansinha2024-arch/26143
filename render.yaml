services:
  - type: web
    name: varuna-netra
    runtime: docker
    plan: free           # 512 MB RAM; sleeps after 15 min idle (see keep-alive note in the guide)
    healthCheckPath: /health
    autoDeployTrigger: commit
    envVars:
      - key: MONGO_URL           # MongoDB Atlas SRV connection string
        sync: false
      - key: DB_NAME
        value: varuna_netra
      - key: JWT_SECRET
        generateValue: true
      - key: ADMIN_EMAIL
        sync: false
      - key: ADMIN_PASSWORD
        sync: false
      - key: DEMO_MODE           # "true" seeds labelled demo data; set "false" for a clean production DB
        value: "true"
      - key: DEMO_SUPERVISOR_EMAIL
        value: supervisor@varuna.example
      - key: DEMO_ANALYST_EMAIL
        value: analyst@varuna.example
      - key: DEMO_SUPERVISOR_PASSWORD
        generateValue: true
      - key: DEMO_ANALYST_PASSWORD
        generateValue: true
      - key: MALLOC_ARENA_MAX
        value: "2"
      - key: WEBHOOK_CRON_SECRET
        generateValue: true
